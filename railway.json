{
  "build": {
    "commands": [
      "mvn clean package -DskipTests"
    ]
  },
  "deploy": {
    "startCommand": "java -Dspring.profiles.active=railway -jar target/servicio-agil-v1-1.0.0.jar",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300
  }
}